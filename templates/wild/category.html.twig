{% block title %}{% endblock %}

{% block body %}


    {% for program in programs %}
    {{ program.id }}
    {{ program.title }}<br>
    {{ program.summary }}<br>


    {% else %}
        Aucune catégorie trouvée.
    {% endfor %}

{% endblock %}